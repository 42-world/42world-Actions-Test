name: action-b

on:
  workflow_run:
    workflows: ["action-a"]
    types:
      - completed

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

needs: action-a

jobs:
  job-b:
    runs-on: ubuntu-latest
    steps:
      - name: Run my script
        id: step1
        run: |
          echo ${{ needs.action-a.outputs.output-a }}
