name: action-a

on: [workflow_call]

jobs:
  job-a:
    runs-on: ubuntu-latest
    outputs:
      output-a: ${{ steps.test.outputs.alpha_version }}
    steps:
      - name: Run my script
        id: test
        run: |
          echo "alpha_version=0.0.1-alpha22" >> $GITHUB_OUTPUT
